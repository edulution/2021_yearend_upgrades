#!/bin/bash

sudo npm -g install knex --save
sudo npm -g install sequelize --save
sudo npm -g install pg --save
sudo npm -g install pg-native --save --unsafe
sudo npm -g install axios --save
sudo npm -g install vue --save
sudo npm -g install webpack --save
