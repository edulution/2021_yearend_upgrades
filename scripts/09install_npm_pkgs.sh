#!/bin/bash

sudo npm -g install n --save

# Install nodejs v10.24.1
sudo n 10

sudo npm -g install forever --save
sudo npm -g install csv --save
sudo npm -g install body-parser --save
sudo npm -g install express --save
sudo npm -g install pg --save
sudo npm -g install knex --save
sudo npm -g install sequelize --save
sudo npm -g install axios --save
sudo npm -g install vue --save
sudo npm -g install webpack --save
